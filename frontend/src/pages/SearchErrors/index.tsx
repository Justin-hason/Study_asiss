import { Typography } from 'antd';

const { Title } = Typography;

export default function SearchErrorsPage() {
  return (
    <div>
      <Title level={2}>搜题错题</Title>
      <p>搜题错题页面（占位）</p>
    </div>
  );
}
