import { Typography } from 'antd';

const { Title } = Typography;

export default function OutlineNotesPage() {
  return (
    <div>
      <Title level={2}>大纲笔记</Title>
      <p>大纲笔记页面（占位）</p>
    </div>
  );
}
